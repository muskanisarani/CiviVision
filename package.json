{
  "name": "civivision-root",
  "version": "1.0.0",
  "description": "Root workspace to launch frontend and backend simultaneously",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
